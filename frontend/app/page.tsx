"use server";
import { redirect } from "next/navigation";
import { LogoutButton } from "@/components/logout-button";

const Home = async() => {

  return (
    <>
    <p>Witaj</p>
    </>
  );
};

export default Home;
